# DHCP Networks config generated by Ralph last modified at {{ last_modified }}

{% spaceless %}
{% for network in entries %}
shared-network "{{ network.name }}" {
    subnet {{ network.network.network_address }} netmask {{ network.netmask_dot_decimal }} {
        option routers {{ network.gateway }};
        option domain-name "{{ network.domain }}";
        {% if network.dns_servers.count %}option domain-name-servers {{ network.dns_servers.all|join:',' }};{% endif %}
        deny unknown-clients;
    }
}{% endfor %}
{% endspaceless %}
# End of autogenerated config
