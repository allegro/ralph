# DHCP config generated by Ralph last modified at {{ last_modified }}
{% for entry in entries %}{% if entry.mac %}
host {{ entry.hostname }} {option host-name {{ entry.hostname }}; fixed-address {{ entry.address }}; hardware ethernet {{ entry.mac }};{% if entry.deployment %} filename "http://{{ ralph_instance_ip }}{% url 'deployment_ipxe' entry.deployment.id %}";{% endif %} }{% endif %}{% endfor %}
# End of autogenerated config
