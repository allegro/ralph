# DHCP config generated by Ralph last modified at {{ last_modified }}
{% for entry in entries %}{% if entry.mac %}
host {{ entry.hostname }} {fixed-address {{ entry.address }}; hardware ethernet {{ entry.mac }};{% if entry.deployment %}{% url 'deployment_ipxe' entry.deployment %}{% endif %}}{% endif %}{% endfor %}
# End of autogenerated config
