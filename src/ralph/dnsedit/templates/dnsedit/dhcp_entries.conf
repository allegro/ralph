# DHCP config generated by Ralph last modified at {{ last_modified_date }}
{% spaceless %}
{% for name, ip_address, mac, next_server in entries %}
host {{ name }} { fixed-address {{ ip_address }}; hardware ethernet {{ mac }};{% if next_server %} next-server {{ next_server }};{% endif %} }{% endfor %}
{% endspaceless %}
# End of autogenerated config
