# DHCP Networks config generated by Ralph last modified at {{ last_modified_date }}
{% spaceless %}
{% for name, subnet, netmask, gateway, domain, additional_config, custom_dns_servers in networks %}
shared-network "{{ name }}" {
    subnet {{ subnet }} netmask {{ netmask }} {
        option routers {{ gateway }};
        option domain-name "{{ domain }}";
        option domain-name-servers {% if custom_dns_servers %}{{ custom_dns_servers }}{% else %}{{ dns_servers }}{% endif %};
        deny unknown-clients;{% if additional_config %}
{{ additional_config|safe }}{% endif %}
    }
}
{% endfor %}
{% endspaceless %}
# End of autogenerated config
