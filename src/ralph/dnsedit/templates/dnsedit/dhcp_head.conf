# DHCP Networks config generated by Ralph at {{ last_modified_date }}
{% spaceless %}
{% if dhcp_server_config %}{{ dhcp_server_config|safe }}{% endif %}

{% for name, subnet, netmask, gateway, domain, additional_config in networks %}
shared-network "{{ name }}" {
    subnet {{ subnet }} netmask {{ netmask }} {
        option routers {{ gateway }};
        option domain-name "{{ domain }}";
        option domain-name-servers {{ dns_servers }};
        deny unknown-clients;{% if additional_config %}
{{ additional_config|safe }}{% endif %}
    }
}
{% endfor %}
{% endspaceless %}
# End of autogenerated config
