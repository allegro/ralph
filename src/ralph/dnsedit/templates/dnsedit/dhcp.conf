# DHCP config generated at {{ last_modified_date }}
{%   spaceless %}
{% for name, address, mac in entries %}
host {{ name }} { fixed-address {{ address }}; hardware ethernet {{ mac }}; }{%   endfor %}
{% endspaceless %}
# End of autogenerated config
